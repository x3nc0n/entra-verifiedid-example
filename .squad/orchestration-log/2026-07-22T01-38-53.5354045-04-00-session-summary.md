# Orchestration Log

- Timestamp: 2026-07-22T01:38:53.5354045-04:00
- Agent: scribe
- Requested by: x3nc0n
- Summary: Merged pending decision inbox entries, updated agent histories, and recorded the full live Azure deployment recovery chain through the successful staging deployment.

## Session summary

- PR #1 merged with the ARM validation fix and npm audit fixes.
- Bug 1: centralus exhausted capacity for the Container Apps environment path, so the deployment flow added a workflow-level region override first and then a dedicated containerAppLocation split so shared infra could stay in the base region while Container Apps moved to eastus2.
- Bug 2/3: bootstrap placeholder image pulls were unreliable through Docker Hub, so the stack switched to the MCR Docker Hub mirror and then corrected the tag to mcr.microsoft.com/mirror/docker/library/node:20-bookworm-slim after 20-alpine proved unavailable there.
- Bug 4: Key Vault secret resolution created an RBAC circular dependency because the bootstrap Container App needed secrets wired during create while its own system identity still lacked Key Vault Secrets User; the fix deferred Key Vault-backed secret wiring to a post-deploy Azure CLI step in .github/workflows/deploy.yml.
- Bug 5: the same deadlock pattern reappeared for ACR pull wiring because configuration.registries[] plus system-identity auth required AcrPull before the app existed; the fix likewise deferred ACR registry wiring to a post-deploy CLI step after the Container App identity was materialized.
- Bug 6: the deploy UAMI only had Contributor and therefore could not create Bicep role assignments; it was granted Role Based Access Control Administrator at resource-group scope live via az CLI and the bootstrap script was updated so future runs reproduce that permission.
- Final result: infrastructure deployment succeeded, app deployment to staging succeeded, smoke tests passed, and the app is live and healthy at https://entra-vid-app.blueground-45957499.eastus2.azurecontainerapps.io/ with both /health and / returning HTTP 200.
- Outstanding: scripts/08-grant-app-uami-graph-permissions.ps1 was not completed because it requires interactive device-code sign-in by a tenant Global Administrator or Privileged Role Administrator; DEMO_MODE=True keeps the app reachable, but real Verified ID issuance/presentation flows still will not work until that admin consent step is done.
- Outstanding: the GitHub production environment was intentionally never configured in scope for this session, so the Deploy -> Production job failed fast on missing AZURE_CLIENT_ID / AZURE_TENANT_ID variables for that environment; only staging was meant to be live today.
