# Project Context

- **Owner:** x3nc0n
- **Project:** entra-verifiedid-example — Microsoft Entra Verified ID employee/guest onboarding demo portal
- **Stack:** Node.js/Express, EJS views, Azure Bicep (Container Apps, Key Vault), GitHub Actions CI/CD
- **Created:** 2026-07-21T13:45:50.478-04:00 (joined mid-assignment, casting universe: The Matrix)

## Learnings

<!-- Append new learnings below. Each entry is something lasting about the project. -->
- Joined after Morpheus rejected Trinity's Container Apps migration (2026-07-21): registry strategy was broken (GHCR image not pullable by ACA, no AcrPull wiring), and `azuredeploy.json` shipped a placeholder `node:20-alpine` image instead of the real app. Full verdict: `.squad/decisions/inbox/morpheus-container-apps-review.md` (once merged, `.squad/decisions.md`).
- Per Reviewer Rejection Lockout: Trinity is locked out of this specific artifact set (deploy.yml, azuredeploy.json, validate.yml, infra registry config) — I own the revision, not Trinity.
- 2026-07-21: Reworked delivery onto Azure Container Registry with managed-identity pull, moved deploy-time GitHub variables to environment scope, and made ARM/Bicep deploys explicitly bootstrap-only until CI publishes the real app image.

- 2026-07-21: FYI from Neo — runtime env names are now standardized on `src/config.js`; Container App docs/vars should use `VC_*`, `IDENTITYPASS_API_ENDPOINT`, and the existing `AZURE_*` / `FIDO2_*` contract.
- 2026-07-21: Live Azure bootstrap exposed two PowerShell/Azure CLI edge cases in `scripts/07-bootstrap-github-actions-uami.ps1`: `"$var:suffix"` inside double-quoted strings must use `${var}` to avoid drive-qualified parsing, and `az role assignment list` must not be passed `--assignee-principal-type` even though `create` accepts it.
- 2026-07-21: Follow-up bootstrap hardening added required non-interactive confirmation flags to destructive delete/cleanup commands so reruns do not hang in headless sessions.
- 2026-07-21: Bootstrap-chain alignment follow-up — `scripts/05-deploy-infrastructure.ps1` is now infra/runtime-config only for Container Apps + ACR; no ZIP/App Service deploy path remains, and `.env` generation follows `src/config.js` names.
- 2026-07-21: Applied Tank's Entra script security fixes in `scripts/01-04` — app-only Graph permission corrected to `User.Read.All`, stale `/signin-oidc` config removed, IdentityPass callback aligned to `/api/identitypass/callback`, DID domain validation tightened, contract minimized to issuance claims, and FIDO2/TAP all-users rollout now requires `-AllowAllUsers`.
- 2026-07-21: Followed Neo's runtime-auth decision — stopped generating/injecting `AZURE_CLIENT_SECRET` for the Container App managed-identity path, and updated `scripts/05-deploy-infrastructure.ps1` to grant Graph + Verified ID request app roles directly to the Container App managed identity.
- 2026-07-21: Split runtime Graph auth onto a dedicated app UAMI (`uami-entra-vid-app`) while keeping the Container App system identity for ACR/Key Vault, added `scripts/08-grant-app-uami-graph-permissions.ps1`, and standardized the runtime contract on `AZURE_CLIENT_ID=<app UAMI clientId>` so Neo can pin `managedIdentityClientId`.
- 2026-07-21: Fixed DemoMode bootstrap regressions by swapping the invalid `<env-hash>` DID placeholder for a valid demo hostname, skipping runtime-UAMI app-role grants in DemoMode, and correcting the `New-Guid` TAP syntax bug in `scripts/06-seed-demo-data.ps1`.
- 2026-07-21: Templatized the public-repo Azure bootstrap story by removing real tenant/subscription defaults from scripts, requiring explicit live-run IDs, and replacing README/examples with `<your-tenant-id>` / `<your-subscription-id>` placeholders.
- 2026-07-21: Fixed the second DemoMode bootstrap regressions by guarding `Get-MgContext` behind DemoMode in `scripts/02-configure-verified-id.ps1` (with a clear disconnected-Graph error for real runs) and by making the `$schema` demo-data key literal in `scripts/06-seed-demo-data.ps1`.
- 2026-07-21: Privileged GitHub Actions workflows in this repo must not mention manual Graph-consent steps at all; `scripts/08-grant-app-uami-graph-permissions.ps1` guidance belongs only in README/local runbooks, not workflow output or comments.
- 2026-07-21: Fixed stale IdentityPass callback drift in `scripts/06-seed-demo-data.ps1` by replacing the obsolete `/api/identitypass/approval*` and `/api/identitypass/webhook` demo config endpoints with the single canonical `/api/identitypass/callback` route used by `scripts/03-configure-identitypass.ps1` and `src/routes/identitypass.js`.
- 2026-07-22T09:32:02.907-04:00: Azure delivery is now fully complete end-to-end for staging: infrastructure, app deployment, and runtime Graph/Verified ID app-role grants are all verified live. The app-runtime UAMI (`uami-entra-vid-app`) now has all four required Microsoft Graph and Verified ID Request Service roles, closing the last delivery blocker.
- 2026-09-10: Provisioned Spaid tenant v2 self-service Verified ID prerequisites additively: created contract `EmployeeOnboardingV2` (`5b70ef0d-b561-9edf-9f32-e6a0940f47b4`) under the existing Quick setup authority, created manager sign-in app `Entra VID Manager Approval v2` (`fda91346-6e00-4078-b89f-f684b476ebfe`) with only the `/auth/manager/callback` redirect and no API permissions, granted runtime UAMI the granular TAP/passkey Graph roles plus the two Verified ID Request Service roles, and created table `onboardingV2Requests` with table-scoped `Storage Table Data Contributor`. Key Vault RBAC blocked storing the manager app secret, so the unstored app credential was deleted and the app currently has no credentials. No ACS/Email Communication resource exists; Graph mail would require explicit `Mail.Send`/mailbox approval.
- 2026-09-11: After approval, temporarily granted operator `Key Vault Secrets Officer` on `entra-vid-kv-feyizvcseko`, provisioned ACS Email pilot resources (`entra-vid-email-v2-feyizvcseko`, `AzureManagedDomain`, `entra-vid-acs-v2-feyizvcseko`), stored `acs-email-connection-string`, `acs-email-endpoint`, and `acs-email-sender-address` in Key Vault, wired those as Key Vault-backed Container App secrets, and removed the temporary Key Vault role. No narrow ACS sender RBAC role was exposed in this subscription, so no runtime UAMI ACS role was granted; ACS send should use the Key Vault-backed connection string. Manager app credential creation remains blocked by Entra directory permissions: the existing manager app has no owner/credential, credential reset and replacement-app creation failed with insufficient privileges, and no manager credential secret is valid yet. Active Container App revision is healthy with v2 disabled, but resource provisioning state is failed from rejected manager-credential update attempts.
- 2026-09-11: Follow-up approval to temporarily assign `Cloud Application Administrator` could not be completed because the operator lacks privilege to add itself to the directory role (`Authorization_RequestDenied`). Manager app `fda91346-6e00-4078-b89f-f684b476ebfe` therefore still has no password/certificate credential and no valid manager credential Key Vault secret. Container App active revision `entra-vid-app--0000013` remains active/running/healthy with 100% traffic and `SELF_SERVICE_V2_ENABLED=false`; visible config has only ACS v2 secret refs plus inert manager values, but the resource `provisioningState` remains `Failed` because Container Apps rejects all updates against a stale hidden `manager-app-client-certificate` validation reference. Stopped before a forced template/revision rewrite per user downtime-risk instruction. Rotated ACS primary key after `az containerapp secret show` exposed the connection string, updated `acs-email-connection-string`, and verified temporary Key Vault Secrets Officer access was removed.
- 2026-09-11: Polled for John's promised external `Cloud Application Administrator` assignment to operator `1ec48eb0-77e6-4637-800c-c9ce6bb4642f`; active role and direct role-assignment checks still show only Security Operator, Helpdesk Administrator, Message Center Reader, and Message Center Privacy Reader. Manager app credentialing remains blocked until the external directory-role grant appears or a directory admin directly creates the credential.
- 2026-09-11: After John's external `Cloud Application Administrator` grant propagated, added manager app credential `v2-manager-app-keyvault-secret` to `Entra VID Manager Approval v2` (`fda91346-6e00-4078-b89f-f684b476ebfe`) and stored it as Key Vault secret `manager-app-client-secret`. Added Container App secret refs for `manager-app-client-secret` and placeholder `manager-app-client-certificate` to satisfy stale control-plane validation, clearing Container App `provisioningState` to `Succeeded`. Deactivated failed generated revision `entra-vid-app--0000014` and reactivated healthy v1 revision `entra-vid-app--0000013`; public `/health` returns HTTP 200 and `SELF_SERVICE_V2_ENABLED=false`. Removed temporary `Cloud Application Administrator` and `Privileged Role Administrator`; operator roles are back to Security Operator, Helpdesk Administrator, Message Center Reader, and Message Center Privacy Reader, with no Key Vault Secrets Officer assignment. Manager env-var wiring is deferred because it would force another Container App template revision.
- 2026-09-11: `az containerapp update --set-env-vars` REPLACES the whole env list rather than merging, so a deploy workflow that passes only v1 vars silently wipes every manually wired v2/ACS var off the live app. Any future env change must be made in `.github/workflows/deploy.yml` as a complete authoritative list, not as an additive patch on the live resource.
- 2026-09-11: `vars.AZURE_CLIENT_ID` in this repo's GitHub environments is the OIDC *deploy* identity used by `azure/login`, not the app runtime identity. The runtime UAMI is carried separately as `vars.AZURE_RUNTIME_CLIENT_ID` and mapped into the container as `AZURE_CLIENT_ID`. Do not conflate them.
- 2026-09-11: Neo's `createAcsEmailClient` checks `if (settings.endpoint)` first and always uses `DefaultAzureCredential` when the endpoint is set, with no connection-string fallback. Since no narrow ACS email-sender RBAC role exists in this subscription (only broad `Communication and Email Service Owner`) and the runtime UAMI has no ACS assignment, `V2_ACS_EMAIL_ENDPOINT` must stay unset so the connection-string branch is used.
- 2026-09-11: With explicit approval for a controlled revision, wired `MANAGER_APP_CLIENT_ID`, `MANAGER_APP_CLIENT_SECRET=secretref:manager-app-client-secret`, and `MANAGER_APP_REDIRECT_URI` into Container App revision `entra-vid-app--0000016`, restored required v1 `AZURE_STORAGE_TABLE_ENDPOINT`, and kept `SELF_SERVICE_V2_ENABLED=false`. Final revision `0000016` is healthy/running with 100% traffic in single-revision mode and Container App `provisioningState=Succeeded`; `/health`, `/`, `/onboarding`, `/onboarding/invite`, `/recovery`, and `/recovery/invite` all return HTTP 200. Removed Container App placeholder secret ref and deleted Key Vault placeholder `manager-app-client-certificate` using temporary `Key Vault Secrets Officer`, then removed that role. Final operator roles remain Security Operator, Helpdesk Administrator, Message Center Reader, and Message Center Privacy Reader only.
- 2026-09-11: Fixed `.github/workflows/deploy.yml` so a merge to `main` deploys the full v1+v2 config instead of wiping v2. Commit `0e5e0db` made both `deploy-staging` and `deploy-production` declare every Key Vault-backed secret and pass an authoritative combined env list (including the previously missing `AZURE_STORAGE_TABLE_ENDPOINT`); commit `5fb0622` removed `V2_ACS_EMAIL_ENDPOINT` so ACS email authenticates via the connection-string secret rather than unauthorized managed identity. `SELF_SERVICE_V2_ENABLED` and `ASSURANCE_MODE` are now GitHub environment variables (`false` and `invitation` in both staging and production) so ordinary merges cannot flip v2. Added `AZURE_RUNTIME_CLIENT_ID` plus the v2 plain vars to both environments, corrected stale `PILOT_GROUP_ID` drift to the live value, and created Key Vault secrets `v2-transient-protection-key` and `v2-verified-id-callback-api-key` using temporary Secrets Officer access that was removed afterward. Workflow was not run and nothing was merged.
- 2026-09-11: Re-investigated ACS Managed Identity authorization per user request. Verified that ACS data-plane accepts Microsoft Entra ID Bearer tokens (`aud: https://communication.azure.com`) evaluated against RBAC role assignments on the individual Communication Services resource. Assigned built-in `Communication and Email Service Owner` (`09976791-48a7-449e-bb21-39d1a415f350`) to runtime UAMI `uami-entra-vid-app` (`379e9964-6827-4dbc-a10c-2cc23e7a3693`), scoped strictly to resource `/subscriptions/7e1b60b8-d616-4396-9de2-fc917930d02e/resourceGroups/rg-entra-verifiedid-example/providers/Microsoft.Communication/CommunicationServices/entra-vid-acs-v2-feyizvcseko`. Validated live email send via REST API with Entra token (operation status `Succeeded`). Updated `.github/workflows/deploy.yml` in commit `9377cc4` to set `V2_ACS_EMAIL_ENDPOINT=secretref:acs-email-endpoint` and removed `V2_ACS_EMAIL_CONNECTION_STRING`, enabling clean Managed Identity auth via `DefaultAzureCredential`.
